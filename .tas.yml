---
framework: jest
postMerge:
  pattern:
    - './src/**/*.spec.ts'
preMerge:
  pattern:
    - './src/**/*.spec.ts'
preRun:
  command:
    - npm ci
postRun:
  command:
    - node --version
version: 2.0
